{
  "name": "winline",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --outfile dist/winline statusline.ts",
    "dev": "bun run statusline.ts",
    "install:local": "bun run build && cp dist/winline ~/.local/bin/winline"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "smol-toml": "^1.3.1"
  }
}
